Step 1 - Open the Appending Python Script

Step 2 - Five the input Zip file Path to it

Step 3 - Run the script (You can use Google Colab,Jupyter Notebook) Anything to run this Script


Assumptions:
	1. If input file name is 'Asia1 prod.csv' -> Output would be 'Asia1 prod' (Not that much Constraints was Mentioned)
	2. If input file name is 'Asia prod100002.csv' -> Output would be 'Asia prod' 
	3. Assuming the Data redundancy will occur in Source IP column. 